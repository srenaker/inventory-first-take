# spec/controllers/application_controller_spec.rb
require 'spec_helper'

describe Application do
  it 'correctly detects a logged-in user' do
    user = FactoryGirl.build(:user)
    
  end
  
  it 'assigns the logged-in user to the @current_user var'
  it 'constructs a user_name'
  
end